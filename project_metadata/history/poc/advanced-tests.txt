=== the five classes, each on its own =============================
   1  FAIL  got BLOCK  want ADMIT  class 1  self-built, signed
          | Error from server: error when creating "STDIN": admission webhook "ivpol.validate.kyverno.svc-ignore-finegrained-platform-image-allowlist" denied the request: Policy platform-image-allowlist failed: This pod contains an image that is neither platform infrastructure, nor a signed self-built image, nor covered by an approved PolicyException. Self-built images: push through CI to the soe/ prefix and they are signed automatically. Vendor images and non-compliant builds: raise an exemption with a ticket and an approver — the organisation does not sign third-party images, so an exception is the only route.
   2  ok    BLOCK  class 2  self-built, unsigned
   3  FAIL  got BLOCK  want ADMIT  class 3  self-built, unsigned, EXEMPTED
          | Error from server: error when creating "STDIN": admission webhook "ivpol.validate.kyverno.svc-ignore-finegrained-platform-image-allowlist" denied the request: Policy platform-image-allowlist failed: This pod contains an image that is neither platform infrastructure, nor a signed self-built image, nor covered by an approved PolicyException. Self-built images: push through CI to the soe/ prefix and they are signed automatically. Vendor images and non-compliant builds: raise an exemption with a ticket and an approver — the organisation does not sign third-party images, so an exception is the only route.
   4  ok    BLOCK  class 4  vendor, not exempted
