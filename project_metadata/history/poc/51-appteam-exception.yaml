##############################################################################################
# APP-TEAM EXEMPTION — the only object an app team contributes to admission control.
#
#     kubectl apply -f 51-appteam-exception.yaml
#
# It is applied by the GitOps controller after review, not by the team directly: see the RBAC
# block at the end of 50-platform-admission.yaml. The team's actual workflow is a pull request
# against the exemptions repo, from which this file is rendered.
#
# WHAT AN APP TEAM MAY AND MAY NOT DO
#   MAY  exempt a specific vendor image, by exact reference, in their own namespaces
#   MAY  exempt a non-compliant self-built image under the unverified/ prefix, same way
#   MAY  sign a compliant self-built image — CI does it, and no exemption is needed
#   NOT  exempt anything under the soe/ prefix. That is Policy A, and nothing may except it:
#        an unsigned image there means the build failed compliance or bypassed CI.
#   NOT  exempt an image in a namespace they do not own — the namespace clause below is what
#        stops this team's exemption carrying a pod in another team's namespace.
#
# WHY THIS TARGETS ONLY platform-image-allowlist
# A PolicyException skips a policy for the whole RESOURCE. If this named
# platform-soe-signature, an approved vendor image would switch signature verification off for
# every image in the same pod — the exemption for a sidecar becomes an exemption for the
# application next to it. Naming only the allowlist policy is what keeps the signature
# requirement real. This is asserted in unikube/tests/test_kyverno_render.py, and it is worth
# knowing that it is an invariant maintained by the RENDERER, not by Kyverno: nothing in the
# CRD stops someone writing the other policy name here.
##############################################################################################
---
apiVersion: policies.kyverno.io/v1
kind: PolicyException
metadata:
  name: demo-approved-images
  namespace: kyverno-exceptions
  labels:
    app.kubernetes.io/managed-by: gitops
    platform.unikube.io/team: demo
    cmdb-app-service-id: "REPLACE-ME"
    # Metadata only. NOTHING IN A v1.18.2 CLUSTER READS THIS. spec.expiresAt is declared on
    # the CRD and accepted by the API server, but the engine does not act on it, so emitting
    # it would be worse than this label — a reviewer who sees expiresAt in the object
    # reasonably concludes the exemption expires. It does not. Expiry is enforced at RENDER
    # time: CI drops entries past their date, so "expired" means "expired at the last render".
    exemption.unikube.io/expires: "2026-11-15"
  annotations:
    exemption.unikube.io/reason: >-
      Vendor observability sidecar and one legacy application image pending a base-image
      upgrade. Neither can carry an organisation signature: the first is third-party, the
      second fails the compliance check and is deliberately not signed.
    exemption.unikube.io/ticket: "SEC-4471"
    exemption.unikube.io/approved-by: "security-team"
    exemption.unikube.io/review-by: "2026-11-15"
spec:
  policyRefs:
    # THE ALLOWLIST POLICY, AND NOTHING ELSE. Never platform-soe-signature.
    - name: platform-image-allowlist
      kind: ImageValidatingPolicy

  # NOT SET, deliberately: spec.images, spec.allowedValues, spec.expiresAt, spec.properties.
  # All four exist on the v1.18.2 CRD and all four are accepted by the API server. None of
  # them does anything for an ImageValidatingPolicy — the CEL variable that exposes the first
  # two, `exceptions`, is not registered in the ivpol environment at all, and the engine does
  # not act on expiresAt. Setting spec.images here would produce an object that LOOKS scoped
  # to two images while still exempting the whole pod. The image match therefore lives in
  # matchConditions, by hand, which is why the expression below is as long as it is.

  matchConditions:
    # 1. NAMESPACE. First and cheapest, and it is what makes app-team exemptions independent
    #    of each other: this team's list never has to mention another team's images, and a
    #    pod elsewhere is never carried by this object. Without it every guard clause would
    #    have to enumerate every other team's exemptions, or a pod mixing two teams' approved
    #    images would match neither exception and be denied.
    - name: demo-namespaces-only
      expression: >-
        object.metadata.namespace in ['demo', 'demo-uat']

    # 2. SELECT. At least one image in the pod is one THIS exemption is about. Without it,
    #    every exemption on the cluster would match every pod made entirely of approved
    #    images, and the report would name an arbitrary one.
    - name: contains-an-approved-image
      expression: |-
        [['891377217246.dkr.ecr.ap-southeast-2.amazonaws.com/vendor-demo/tool:3.0', '891377217246.dkr.ecr.ap-southeast-2.amazonaws.com/vendor-demo/sidecar:1.0', '891377217246.dkr.ecr.ap-southeast-2.amazonaws.com/unverified/app:3.0']].all(exempt,
        [(has(object.spec.jobTemplate) ? object.spec.jobTemplate.spec.template.spec : (has(object.spec.template) ? object.spec.template.spec : object.spec))].all(s,
          s.containers.exists(c, c.image in exempt) ||
          (has(s.initContainers) ? s.initContainers : []).exists(c, c.image in exempt) ||
          (has(s.ephemeralContainers) ? s.ephemeralContainers : []).exists(c, c.image in exempt)))

    # 3. GUARD. EVERY image in the pod must be platform infrastructure, under the SOE prefix,
    #    or on this team's approved list. This is the clause that stops an exemption for one
    #    image becoming an exemption for whatever is shipped beside it — the failure the whole
    #    design exists to prevent, and the easiest one to drop when simplifying.
    #
    #    SOE images pass the guard on purpose. They remain Policy A's business, and Policy A
    #    is unexceptable — so an UNSIGNED image under soe/ in this pod is still denied even
    #    though this exception matched. That is the property being relied on, and it is why
    #    "never reference the signature policy" is load-bearing rather than stylistic.
    #    poc/cel_check.py checks it: case [5, 2] — approved vendor image plus unsigned
    #    self-built image — is BLOCKED.
    #
    #    The prefix list here MUST stay identical to the one in platform-image-allowlist's
    #    matchConditions. They are rendered from one source for that reason; hand-editing one
    #    of them produces a pod that the allowlist denies and no exception can rescue.
    - name: every-other-image-is-accounted-for
      expression: |-
        [['602401143452.dkr.ecr.ap-southeast-2.amazonaws.com/eks/', 'public.ecr.aws/eks-distro/', 'public.ecr.aws/ebs-csi-driver/', 'registry.k8s.io/', 'ghcr.io/kyverno/', '891377217246.dkr.ecr.ap-southeast-2.amazonaws.com/soe-demo/', '891377217246.dkr.ecr.ap-southeast-2.amazonaws.com/soe-team2/']].all(allow,
        [['891377217246.dkr.ecr.ap-southeast-2.amazonaws.com/vendor-demo/tool:3.0', '891377217246.dkr.ecr.ap-southeast-2.amazonaws.com/vendor-demo/sidecar:1.0', '891377217246.dkr.ecr.ap-southeast-2.amazonaws.com/unverified/app:3.0']].all(exempt,
        [(has(object.spec.jobTemplate) ? object.spec.jobTemplate.spec.template.spec : (has(object.spec.template) ? object.spec.template.spec : object.spec))].all(s,
          s.containers.all(c, (allow.exists(p, c.image.startsWith(p)) || c.image in exempt)) &&
          (has(s.initContainers) ? s.initContainers : []).all(c, (allow.exists(p, c.image.startsWith(p)) || c.image in exempt)) &&
          (has(s.ephemeralContainers) ? s.ephemeralContainers : []).all(c, (allow.exists(p, c.image.startsWith(p)) || c.image in exempt)))))

  # `skip` rather than `pass`, so exempted workloads stay visibly different from verified ones
  # in `kubectl get polr -A`. Note the measured caveat: under [Deny, Audit] no skip row is
  # written at all — the trail only appears with [Audit] alone. An enforcing cluster keeps no
  # standing record of which exemptions are carrying which workloads, which is an argument for
  # tracking exemptions in the repo rather than in the cluster.
  reportResult: skip
