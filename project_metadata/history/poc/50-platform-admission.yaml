##############################################################################################
# PLATFORM-OWNED ADMISSION CONTROL — everything the platform team applies when it provisions
# a cluster. One file, applied as a unit:
#
#     kubectl apply -f 50-platform-admission.yaml
#
# App teams do not edit this file and cannot weaken it. What they may write is one object,
# in one namespace, targeting one policy: see 51-appteam-exception.yaml.
#
# BEFORE APPLYING, three strings must be right for your account. Everything else is generic.
#   1. the SOE prefixes and UNVERIFIED prefix             -> search `891377217246`
#   2. the EKS addon registry                            -> search `602401143452`
#   3. the Notary root CA PEM                            -> search `PASTE-ROOT-CA-PEM-HERE`
#
# Derive 1 and 2 from the cluster rather than trusting a hardcoded value — the EKS addon ECR
# account differs by region AND partition, and getting it wrong denies your own CNI:
#
#   kubectl -n kube-system get ds,deploy -o jsonpath='{range .items[*]}{range .spec.template.spec.containers[*]}{.image}{"\n"}{end}{range .spec.template.spec.initContainers[*]}{.image}{"\n"}{end}{end}' | sort -u
#
# And fill 3 from the demo PKI with:
#   python3 - <<'EOF'
#   import pathlib, os, re
#   ca = pathlib.Path(os.environ["DEMO_CA"]).read_text().rstrip("\n")
#   pem = "\n".join(" " * 12 + l for l in ca.splitlines())
#   p = pathlib.Path("50-platform-admission.yaml"); t = p.read_text()
#   p.write_text(re.sub(r"^ {12}PASTE-ROOT-CA-PEM-HERE$", pem, t, flags=re.M))
#   EOF
#
##############################################################################################
# THE MODEL, IN ONE PARAGRAPH
#
# Two policies. `platform-soe-signature` says self-built images must be signed, and NOTHING may
# except it. `platform-image-allowlist` matches EVERY image in every pod and denies by default;
# it is the only policy an exception may target. A pod is admitted when every image in it is
# either platform infrastructure, a signed self-built image, or covered by an approved
# exception — and denied otherwise. CoreDNS, kube-proxy and the VPC CNI are in scope of the
# allowlist policy exactly like anything else; they pass because their registries are named in
# its matchConditions, not because anything skipped them.
#
##############################################################################################
# WHY THE SYSTEM-IMAGE ALLOWLIST IS **SCOPE**, NOT A PolicyException
#
# This is the one design decision worth arguing about, and it is the reverse of the obvious
# approach. A PolicyException is per-RESOURCE, not per-image: when it matches, Kyverno skips
# the whole policy for the whole pod. So a PolicyException for a platform sidecar image is not
# an exemption for that image — it is an exemption for every pod that contains it.
#
# Sidecar injection is automatic and universal, which turns that into a bypass primitive:
#
#     pod: [ my-unsigned-app:latest , <injected platform sidecar> ]
#            ^ never signed, never approved      ^ "exempted" by the platform
#     -> exception matches the POD -> allowlist policy skipped -> BOTH admitted
#
# Every app team that can get a sidecar injected can then ship anything. The more standard the
# sidecar, the more universal the hole. Put the platform allowlist in matchConditions instead
# and excluding an image excludes only that image. Reserve PolicyException for what its shape
# actually fits: a decision about one workload, with a ticket and an approver.
#
# `poc/cel_check.py` proves this holds — case [6, 2] (platform sidecar + unsigned self-built)
# is BLOCKED, across all 258 pods of one to three containers over the six image classes.
##############################################################################################
---
apiVersion: v1
kind: Namespace
metadata:
  name: kyverno-exceptions
  labels:
    app.kubernetes.io/managed-by: platform-team
    cmdb-app-service-id: "REPLACE-ME"
  annotations:
    platform.unikube.io/description: >-
      The ONLY namespace Kyverno reads PolicyExceptions from. Set
      features.policyExceptions.namespace to this value at install time; leaving it at '*'
      lets any namespace-admin write unrestricted CEL and exempt anything, anywhere.

---
##############################################################################################
# POLICY A — SELF-BUILT IMAGES MUST BE SIGNED.
#
# UNEXCEPTABLE BY CONSTRUCTION. No PolicyException in this design names this policy, and the
# renderer's test suite asserts it. That is what makes the signature requirement a real
# guarantee rather than a default: for anything under the SOE prefix there is no paperwork
# route to admission, only a signature.
#
# NOTE THERE IS NO EXEMPTION LIST IN THIS POLICY, and that is deliberate. An earlier design
# carved exempted images out of the scope expression, which works but drags app-team data into
# a platform-owned object: every exemption edit re-renders this policy on every cluster, bumps
# its resourceVersion, and cold-starts imageVerifyCache fleet-wide. Instead the SOE prefix is
# an ACL BOUNDARY — only CI can push there, and CI signs everything it pushes, so "unsigned
# image under the SOE prefix" is impossible rather than merely disallowed. A non-compliant
# self-built image goes to <ECR>/unverified/ and is handled exactly like a vendor image: by an
# exception against Policy B. This policy is then STATIC — it never changes when an exemption
# does, which at ~200 clusters is the difference between a config change and a fleet event.
##############################################################################################
apiVersion: policies.kyverno.io/v1
kind: ImageValidatingPolicy
metadata:
  name: platform-soe-signature
  labels:
    app.kubernetes.io/managed-by: platform-team
    cmdb-app-service-id: "REPLACE-ME"
  annotations:
    policies.kyverno.io/description: >-
      Images under the SOE prefix must carry a Notation signature chaining to the organisation
      root CA. No PolicyException may reference this policy.
spec:
  # Deny rejects the request; Audit is what writes the PolicyReport. Under [Deny, Audit] only
  # DENYING results are reported — measured. Start a rollout at [Audit] alone for a quarter,
  # harvest the real image inventory from the reports, then switch to this.
  validationActions:
    - Deny
    - Audit
  # Ignore, not Fail. Fail-closed admission stops every pod create in the cluster during a
  # Kyverno or registry outage — a strictly larger blast radius than admitting unsigned
  # images for the duration. It is also the open door in this design: during an outage,
  # enforcement is off. Choose deliberately and write down which you chose.
  failurePolicy: Ignore
  webhookConfiguration:
    timeoutSeconds: 15
  evaluation:
    admission:
      enabled: true
    # OFF. Background scan re-verifies every matched image from the REPORTS controller, a
    # second registry-egress path needing its own IRSA role. Turn on deliberately.
    background:
      enabled: false
  autogen:
    podControllers:
      controllers: [deployments, jobs, cronjobs, statefulsets, daemonsets, replicasets]
  matchConstraints:
    resourceRules:
      - apiGroups: [""]
        apiVersions: ["v1"]
        operations: ["CREATE", "UPDATE"]
        resources: ["pods"]
  # A plain glob, because the prefix is an access-control boundary (see the note above).
  #
  # DO NOT widen this to '<account>.dkr.ecr.<region>.amazonaws.com/*'. It looks equivalent —
  # it is all one registry — and it is not: an ECR pull-through cache rule materialises
  # upstream vendor images INSIDE your own registry namespace, which would pull third-party
  # images into a policy nothing can except them from.
  matchImageReferences:
    - glob: "891377217246.dkr.ecr.ap-southeast-2.amazonaws.com/soe-demo/*"
    - glob: "891377217246.dkr.ecr.ap-southeast-2.amazonaws.com/soe-team2/*"
  # REQUIRED for ECR. The chart-level features.registryClient.credentialHelpers is NOT enough
  # on its own — an IVP carries its own credentials block, and without it the admission
  # controller GETs the manifest anonymously and takes a 401.
  credentials:
    allowInsecureRegistry: false
    providers:
      - default
      - amazon
  attestors:
    - name: notary
      notary:
        # The ROOT, not the intermediate: pinning an intermediate is certificate pinning that
        # breaks on every intermediate rotation. tsaCerts deliberately unset — signatures are
        # not trust-timestamped, so an expired leaf stops admitting.
        certs:
          value: |
            PASTE-ROOT-CA-PEM-HERE
  validations:
    - expression: >-
        images.containers.all(image, verifyImageSignatures(image, [attestors.notary]) > 0)
      message: >-
        Image is under the SOE prefix and is not signed by the organisation root CA. CI signs
        every compliant build automatically. There is no exception route for this policy — an
        unsigned image here means the build did not pass compliance, or it was pushed outside
        CI. Non-compliant builds belong under the unverified/ prefix with an approved
        PolicyException.
    - expression: >-
        images.initContainers.all(image, verifyImageSignatures(image, [attestors.notary]) > 0)
      message: >-
        Init container image is under the SOE prefix and is not signed by the organisation
        root CA.
    - expression: >-
        images.ephemeralContainers.all(image, verifyImageSignatures(image, [attestors.notary]) > 0)
      message: >-
        Ephemeral container image is under the SOE prefix and is not signed by the
        organisation root CA.
  validationConfigurations:
    # MEASURED INERT on v1.18.2 — the pod keeps its tag. Deploy by digest if you need pinning
    # on this version.
    mutateDigest: true
    verifyDigest: true
    # FALSE, and not because it is inert on 1.18. `required` denies any image no policy
    # verified, and it is not itself a policy — so no PolicyException can target it, and an
    # approved vendor image would be denied with nothing to except it from. Policy B is what
    # exemptions are excepted FROM, and it does this job exceptably.
    required: false

---
##############################################################################################
# POLICY B — EVERY IMAGE IN EVERY POD, DENIED UNLESS ACCOUNTED FOR.
#
# glob "**" — CoreDNS, kube-proxy, the VPC CNI, every vendor sidecar, every typo'd registry
# host. `validations: [false]` means this policy NEVER verifies anything and never touches a
# registry; matching everything is free precisely because of that. Putting
# verifyImageSignatures() behind glob "**" would fan ~4 registry round trips per image out to
# docker.io, quay.io and gcr.io on the pod-create path — coupling pod scheduling to hosts you
# do not control. Deny-by-default and verify-everything are different things; this is the
# first.
#
# matchConditions is where the platform allowlist lives. The policy is SKIPPED when every
# image in the pod is already accounted for — platform infrastructure, or under the SOE prefix
# where Policy A is separately and unexceptably responsible for it. Anything else, and this
# policy applies and denies, unless an app team's PolicyException matches.
#
# THE ALLOWLIST IS BY REPOSITORY PREFIX, NEVER BY TAG OR DIGEST. EKS-managed addons update on
# AWS's schedule, not yours; a tag-pinned entry makes CoreDNS unschedulable on the next addon
# upgrade, and the way you find out is that the cluster cannot resolve DNS.
##############################################################################################
apiVersion: policies.kyverno.io/v1
kind: ImageValidatingPolicy
metadata:
  name: platform-image-allowlist
  labels:
    app.kubernetes.io/managed-by: platform-team
    cmdb-app-service-id: "REPLACE-ME"
  annotations:
    policies.kyverno.io/description: >-
      Deny-by-default over every image. Platform infrastructure passes by allowlist; SOE
      images are Policy A's business; everything else needs an approved PolicyException.
spec:
  validationActions:
    - Deny
    - Audit
  failurePolicy: Ignore
  webhookConfiguration:
    timeoutSeconds: 15
  evaluation:
    admission:
      enabled: true
    background:
      enabled: false
  autogen:
    podControllers:
      controllers: [deployments, jobs, cronjobs, statefulsets, daemonsets, replicasets]
  matchConstraints:
    resourceRules:
      - apiGroups: [""]
        apiVersions: ["v1"]
        operations: ["CREATE", "UPDATE"]
        resources: ["pods"]
  matchImageReferences:
    - glob: "**"
  matchConditions:
    # Apply UNLESS every image in the pod is already accounted for.
    #
    # Reading the CEL: `[[...list...]].all(allow, BODY)` is the let-binding idiom — a
    # one-element list whose .all() macro binds the inner list to `allow`. CEL has no `let`,
    # and repeating the prefix list six times inside one expression is how allowlists come to
    # disagree with themselves. The `[podSpec].all(s, ...)` binder does the same job for the
    # pod spec, which lives at object.spec for a Pod, object.spec.template.spec for a
    # Deployment/StatefulSet/DaemonSet/Job, and object.spec.jobTemplate.spec.template.spec for
    # a CronJob. The ternary handles all three shapes: Kyverno's autogen rewrites the POLICY
    # for pod controllers, but whether it rewrites these matchConditions is unverified, and
    # the cost of being wrong is every Deployment in the cluster denied on day one.
    #
    # `has(s.initContainers) ? s.initContainers : []` rather than Kyverno's own
    # `s.?initContainers.orValue([])` — the latter is the CEL optional-types extension, which
    # Kyverno's evaluator has and standard CEL evaluators do not, so choosing the portable
    # form is what makes poc/cel_check.py able to prove this expression offline at all.
    #
    # THE INIT-CONTAINER CLAUSE IS LOAD-BEARING. The VPC CNI ships an init container; an
    # allowlist that only reads spec.containers denies the CNI on node start.
    - name: not-already-accounted-for
      expression: |-
        !(
        [['602401143452.dkr.ecr.ap-southeast-2.amazonaws.com/eks/', 'public.ecr.aws/eks-distro/', 'public.ecr.aws/ebs-csi-driver/', 'registry.k8s.io/', 'ghcr.io/kyverno/', '891377217246.dkr.ecr.ap-southeast-2.amazonaws.com/soe-demo/', '891377217246.dkr.ecr.ap-southeast-2.amazonaws.com/soe-team2/']].all(allow,
        [(has(object.spec.jobTemplate) ? object.spec.jobTemplate.spec.template.spec : (has(object.spec.template) ? object.spec.template.spec : object.spec))].all(s,
          s.containers.all(c, allow.exists(p, c.image.startsWith(p))) &&
          (has(s.initContainers) ? s.initContainers : []).all(c, allow.exists(p, c.image.startsWith(p))) &&
          (has(s.ephemeralContainers) ? s.ephemeralContainers : []).all(c, allow.exists(p, c.image.startsWith(p))))))
  attestors:
    # Required by the schema even though nothing here will ever verify against it — the
    # validation below is the constant false.
    - name: notary
      notary:
        certs:
          value: |
            PASTE-ROOT-CA-PEM-HERE
  validations:
    - expression: "false"
      message: >-
        This pod contains an image that is neither platform infrastructure, nor a signed
        self-built image, nor covered by an approved PolicyException. Self-built images:
        push through CI to the soe/ prefix and they are signed automatically. Vendor images
        and non-compliant builds: raise an exemption with a ticket and an approver — the
        organisation does not sign third-party images, so an exception is the only route.
  validationConfigurations:
    # This policy never admits anything on its own, so digest pinning would be wasted work and
    # an extra webhook on the admission path.
    mutateDigest: false
    verifyDigest: false
    required: false

---
##############################################################################################
# RBAC — WHO MAY WRITE AN EXCEPTION.
#
# A PolicyException's matchConditions is UNRESTRICTED CEL. Anyone who can create one in this
# namespace can write `true` and exempt the entire cluster, so this binding is the real
# security boundary of the whole design — tighter than either policy above.
#
# Bound to a GitOps service account and NOBODY ELSE, on purpose. App teams do not get write
# access here; they raise a pull request against the exemptions repo, it is reviewed, and the
# controller applies it. Binding this to an app-team group would mean the exception process is
# whatever that team decides it is.
#
# Replace the ServiceAccount below with your delivery controller's identity (Argo CD, Flux,
# the CI role). If you have no GitOps controller yet, leave the binding empty and apply
# exceptions with a break-glass admin credential — an empty binding is a documented gap; an
# app-team binding is a silently open door.
##############################################################################################
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: policyexception-writer
  namespace: kyverno-exceptions
  labels:
    app.kubernetes.io/managed-by: platform-team
    cmdb-app-service-id: "REPLACE-ME"
rules:
  - apiGroups: ["policies.kyverno.io"]
    resources: ["policyexceptions"]
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]

---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: policyexception-writer
  namespace: kyverno-exceptions
  labels:
    app.kubernetes.io/managed-by: platform-team
    cmdb-app-service-id: "REPLACE-ME"
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: policyexception-writer
subjects:
  - kind: ServiceAccount
    name: gitops-controller
    namespace: argocd

---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: policyexception-reader
  namespace: kyverno-exceptions
  labels:
    app.kubernetes.io/managed-by: platform-team
    cmdb-app-service-id: "REPLACE-ME"
  annotations:
    platform.unikube.io/description: >-
      Read-only. Bind this to app-team groups so they can see which exemptions exist and why —
      an exception process nobody can inspect gets worked around rather than followed.
rules:
  - apiGroups: ["policies.kyverno.io"]
    resources: ["policyexceptions"]
    verbs: ["get", "list", "watch"]
